const fs = require('fs');

function generatePixelIcon(size, text) {
  // Creating a simple base64 PNG is hard without deps, but we can write a raw SVG and convert it, 
  // actually let's just make it a valid SVG and change manifest to use svg!
}
